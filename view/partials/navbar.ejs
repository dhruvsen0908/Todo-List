<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand ms-3 fs-3 fw-bold" href="/"><i class="bi bi-list-check"></i> To-Do List</a>

        <!-- if the user is not logged in -->
        <% if(!user_id){ %>
            <div class="d-flex">
                <div class="navbar-nav ms-auto me-2">
                    <a class="btn btn-success" aria-current="page" href="/signin">Sign In</a>
                </div>
                <div class="navbar-nav ms-auto">
                    <a class="btn btn-outline-success" aria-current="page" href="/signup">Sign Up</a>
                </div>
            </div>
            <% }else{ %>

                <!-- if the user is logged in -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false"
                    aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item pt-2 pt-md-3 ms-auto">
                            <a class=" btn btn-outline-danger" aria-current="page" href="/logout">Logout</a>
                        </li>
                    </ul>
                </div>
                <% } %>
    </div>
</nav>